name: Build APK (Fixed Buildozer New Path)

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      # 1️⃣ Checkout repo
      - name: Checkout repo
        uses: actions/checkout@v4

      # 2️⃣ Install system dependencies
      - name: Install system dependencies
        run: |
          sudo apt update
          sudo apt install -y python3-pip git zip unzip openjdk-17-jdk wget

      # 3️⃣ Install Buildozer + Cython
      - name: Install Buildozer & Cython
        run: |
          pip3 install --upgrade buildozer cython

      # 4️⃣ Setup Android SDK & NDK
      - name: Setup Android SDK & NDK
        run: |
          mkdir -p $HOME/.buildozer/android/platform
          cd $HOME/.buildozer/android/platform

          # Install NDK if not exists
          if [ ! -d "android-ndk-r25b" ]; then
            wget https://dl.google.com/android/repository/android-ndk-r25b-linux.zip -O android-ndk.zip
            unzip android-ndk.zip
            rm android-ndk.zip
          fi

          # Install SDK command-line tools with new path
          mkdir -p android-sdk/cmdline-tools
          cd android-sdk/cmdline-tools
          wget https://dl.google.com/android/repository/commandlinetools-linux-9477386_latest.zip -O cmdline-tools.zip
          unzip cmdline-tools.zip
          rm cmdline-tools.zip

          # Move to 'latest' folder (required by Buildozer)
          mkdir -p latest
          mv cmdline-tools/* latest/

      # 5️⃣ Accept SDK licenses + install platform-tools & platforms
      - name: Accept SDK licenses
        run: |
          yes | $HOME/.buildozer/android/platform/android-sdk/cmdline-tools/latest/bin/sdkmanager --licenses
          $HOME/.buildozer/android/platform/android-sdk/cmdline-tools/latest/bin/sdkmanager "platform-tools" "platforms;android-33"

      # 6️⃣ Build APK
      - name: Build APK
        run: |
          cd $GITHUB_WORKSPACE
          buildozer android debug

      # 7️⃣ Optional: list APK output folder
      - name: Show output
        run: |
          ls -R $GITHUB_WORKSPACE/bin
