name: Build APK

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repo
      uses: actions/checkout@v3

    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: "3.11"

    - name: Setup Buildozer venv
      shell: bash
      run: |
        python -m venv buildozer-venv
        source buildozer-venv/bin/activate
        pip install --upgrade pip setuptools wheel
        pip install cython==0.29.36 buildozer python-for-android

    - name: Install Android cmdline-tools + Build-tools + AIDL
      shell: bash
      run: |
        SDK_ROOT=$HOME/.buildozer/android/platform/android-sdk
        mkdir -p $SDK_ROOT/cmdline-tools
        if [ ! -f "$SDK_ROOT/cmdline-tools/latest/bin/sdkmanager" ]; then
          wget -q https://dl.google.com/android/repository/commandlinetools-linux-9477386_latest.zip -O cmdline-tools.zip
          unzip -q cmdline-tools.zip -d $SDK_ROOT/cmdline-tools/
          rm cmdline-tools.zip
          mv $SDK_ROOT/cmdline-tools/cmdline-tools $SDK_ROOT/cmdline-tools/latest
        fi
        export ANDROID_SDK_ROOT=$SDK_ROOT
        yes | $SDK_ROOT/cmdline-tools/latest/bin/sdkmanager --sdk_root=$SDK_ROOT --licenses
        $SDK_ROOT/cmdline-tools/latest/bin/sdkmanager --sdk_root=$SDK_ROOT "build-tools;33.0.2" "platforms;android-33"
        # Ensure aidl exists
        if [ ! -f "$SDK_ROOT/build-tools/33.0.2/aidl" ]; then
          echo "AIDL not found, Build-tools installation failed!" && exit 1
        fi

    - name: Build APK
      shell: bash
      working-directory: ./   # jithe buildozer.spec hai
      run: |
        source buildozer-venv/bin/activate
        python -m buildozer android debug
